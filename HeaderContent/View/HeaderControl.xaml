<UserControl x:Class="HeaderContent.View.HeaderControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HeaderContent.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
  <Grid >
        <Grid.RowDefinitions>
          <RowDefinition Height="20"></RowDefinition>
          <RowDefinition Height="50"></RowDefinition>
    </Grid.RowDefinitions>
     <Grid.ColumnDefinitions>
       <ColumnDefinition Width="200"></ColumnDefinition>
       <ColumnDefinition Width="Auto"></ColumnDefinition>
       <ColumnDefinition Width="20"></ColumnDefinition>
       <ColumnDefinition Width="Auto"></ColumnDefinition>
    </Grid.ColumnDefinitions>
    <Button Background="DarkBlue" Command="{Binding LoadDataCommand}" Foreground="White" Width="100" Grid.Row="1" Grid.Column="1" Content="Load Data"></Button>
    <Button Background="DarkBlue" Command="{Binding AttachFileCommand}" Foreground="White"  Width="120" Grid.Row="1" Grid.Column="3" Content="Attach File For Email"></Button>
    </Grid>
</UserControl>
